# Base URL of the FastAPI backend. Leave unset in dev to default to
# http://localhost:8000. In production (if the frontend is served from a
# different origin than the API) set this to the deployed API's URL.
VITE_API_BASE=http://localhost:8000
